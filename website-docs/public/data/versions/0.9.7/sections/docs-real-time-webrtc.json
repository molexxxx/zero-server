{
	"section": "WebRTC",
	"icon": "video",
	"items": [
		{
			"name": "SignalingHub",
			"description": "WebRTC signaling hub - the central WS broker that owns the room registry, attaches peers, validates JSEP messages, and routes offer / answer / ICE traffic between participants. The hub itself is transport-agnostic: anything that exposes a `{ send(string), on('message'|'close', cb), close(code?, reason?) }` surface is acceptable.  `createWebRTC(app, opts)` (PR 9 wiring layer) binds an `app.ws()` upgrade handler to a `SignalingHub`.",
			"options": [
				{
					"option": "maxSdpSize",
					"type": "number",
					"default": "65536",
					"notes": "Hard cap on offer / answer size (bytes)."
				},
				{
					"option": "maxCandidatesPerOffer",
					"type": "number",
					"default": "30",
					"notes": "Hard cap on `a=candidate:` lines per SDP."
				},
				{
					"option": "peerMessageRate",
					"type": "number",
					"default": "30",
					"notes": "Per-peer signaling message rate, msg / sec."
				},
				{
					"option": "maxProtocolErrors",
					"type": "number",
					"default": "5",
					"notes": "Disconnect after this many malformed frames."
				},
				{
					"option": "ipAttachRate",
					"type": "number",
					"default": "0",
					"notes": "Max attaches per IP per minute.  `0` disables."
				},
				{
					"option": "originAllowlist",
					"type": "string[]",
					"default": "-",
					"notes": "If set, transports whose `info.origin` is not\non this list are rejected at attach time."
				},
				{
					"option": "joinTokenSecret",
					"type": "string | Buffer",
					"default": "-",
					"notes": "If set, every `join` must include a valid\nJWT signed with this secret and audience `room:<name>`."
				},
				{
					"option": "autoCreateRooms",
					"type": "boolean",
					"default": "true",
					"notes": "If false, joins targeting an unknown room are rejected."
				}
			],
			"methodGroups": [
				{
					"group": "Public surface",
					"methods": [
						{
							"method": "size",
							"signature": "size()",
							"description": "Live peer count across all rooms (and unattached)."
						},
						{
							"method": "room",
							"signature": "room(name)",
							"description": "Get or lazily create a room.",
							"methodParams": [
								{
									"param": "name",
									"type": "string",
									"required": "Yes",
									"notes": ""
								}
							],
							"methodReturns": {
								"type": "Room",
								"description": ""
							}
						},
						{
							"method": "rooms",
							"signature": "rooms()",
							"methodReturns": {
								"type": "Room[]",
								"description": ""
							}
						},
						{
							"method": "attach",
							"signature": "attach(transport, [info])",
							"description": "Attach a transport (WS connection or mock) as a new signaling peer. Wires up message and close handlers; returns the `Peer`.",
							"methodParams": [
								{
									"param": "transport",
									"type": "object",
									"required": "Yes",
									"notes": "`{ send, on, close }`-shaped object."
								},
								{
									"param": "info",
									"type": "object",
									"required": "No",
									"notes": ""
								}
							],
							"methodOptions": [
								{
									"option": "user",
									"type": "*",
									"default": "-",
									"notes": "Authenticated user."
								},
								{
									"option": "ip",
									"type": "string",
									"default": "-",
									"notes": "Remote IP."
								}
							],
							"methodReturns": {
								"type": "Peer",
								"description": ""
							}
						}
					]
				},
				{
					"group": "Per-IP attach rate limit",
					"methods": [
						{
							"method": "close",
							"signature": "close()",
							"description": "Shut down every peer and clear every room."
						}
					]
				}
			]
		},
		{
			"name": "Room",
			"description": "Room / channel abstraction for the WebRTC signaling hub. A `Room` holds a set of `Peer`s plus a list of `require()` policy gates that decide whether a given peer may join.  Membership is in-process; the cluster adapter (PR 8) will fan room state out via pub/sub.",
			"options": [
				{
					"option": "hub",
					"type": "import('./signaling').SignalingHub",
					"default": "-",
					"notes": "Owning hub."
				}
			],
			"params": [
				{
					"param": "name",
					"type": "string",
					"required": "Yes",
					"notes": "Room name, used as routing key."
				}
			],
			"methodGroups": [
				{
					"group": "Configuration (fluent)",
					"methods": [
						{
							"method": "open",
							"signature": "open()",
							"description": "Mark the room as public.  Returns `this` for chaining."
						},
						{
							"method": "canPublish",
							"signature": "canPublish(fn)",
							"description": "Set the publish-permission check.  Hub calls this before relaying offers.",
							"methodParams": [
								{
									"param": "fn",
									"type": "(peer:import('./peer').Peer) => boolean",
									"required": "Yes",
									"notes": ""
								}
							]
						},
						{
							"method": "canSubscribe",
							"signature": "canSubscribe(fn)",
							"description": "Set the subscribe-permission check.  Hub calls this before relaying answers.",
							"methodParams": [
								{
									"param": "fn",
									"type": "(peer:import('./peer').Peer) => boolean",
									"required": "Yes",
									"notes": ""
								}
							]
						}
					]
				},
				{
					"group": "Membership",
					"methods": [
						{
							"method": "size",
							"signature": "size()",
							"description": "Current member count."
						},
						{
							"method": "peers",
							"signature": "peers()",
							"methodReturns": {
								"type": "import('./peer').Peer[]",
								"description": ""
							}
						},
						{
							"method": "has",
							"signature": "has()",
							"methodReturns": {
								"type": "boolean",
								"description": ""
							}
						},
						{
							"method": "canJoin",
							"signature": "canJoin(peer)",
							"description": "Evaluate every `require()` gate against the candidate peer.",
							"methodParams": [
								{
									"param": "peer",
									"type": "import('./peer').Peer",
									"required": "Yes",
									"notes": ""
								}
							],
							"methodReturns": {
								"type": "boolean",
								"description": ""
							}
						}
					]
				},
				{
					"group": "Fan-out",
					"methods": [
						{
							"method": "broadcast",
							"signature": "broadcast(type, [payload], [exceptPeerId])",
							"description": "Send a `{type, ...payload}` JSON frame to every peer in the room.",
							"methodParams": [
								{
									"param": "type",
									"type": "string",
									"required": "Yes",
									"notes": ""
								},
								{
									"param": "payload",
									"type": "object",
									"required": "No",
									"notes": ""
								},
								{
									"param": "exceptPeerId",
									"type": "string",
									"required": "No",
									"notes": "Optional peer id to skip (e.g. the originator)."
								}
							]
						},
						{
							"method": "close",
							"signature": "close()",
							"description": "Kick every peer with code 1001 (going-away) and unregister from the hub."
						}
					]
				}
			]
		},
		{
			"name": "Peer",
			"description": "Per-connection state machine for a WebRTC signaling peer. A `Peer` wraps a transport object (typically a `WebSocketConnection` but any duck-typed `{ send, on, close }` works - see the unit tests for a minimal in-memory transport) and exposes the JSEP message types as ordinary events.  The hub is responsible for routing.",
			"options": [
				{
					"option": "user",
					"type": "*",
					"default": "-",
					"notes": "Authenticated user object (if any)."
				},
				{
					"option": "ip",
					"type": "string",
					"default": "-",
					"notes": "Remote IP for audit / rate limits."
				}
			],
			"params": [
				{
					"param": "transport",
					"type": "object",
					"required": "Yes",
					"notes": "Anything with `send(string)`, `on('message'|'close', cb)`, `close(code?, reason?)`."
				},
				{
					"param": "info",
					"type": "object",
					"required": "No",
					"notes": "Connection metadata."
				}
			],
			"methodGroups": [
				{
					"group": "Peers",
					"methods": [
						{
							"method": "send",
							"signature": "send(type, [payload])",
							"description": "Send a JSON envelope to this peer.  `type` is added to `payload` and the result is serialised once.  Silently drops sends after close.",
							"methodParams": [
								{
									"param": "type",
									"type": "string",
									"required": "Yes",
									"notes": ""
								},
								{
									"param": "payload",
									"type": "object",
									"required": "No",
									"notes": ""
								}
							]
						},
						{
							"method": "sendError",
							"signature": "sendError(code, message)",
							"description": "Send a typed error frame.  Callers SHOULD use this rather than throwing, because exceptions escape the message handler and tear down the process.",
							"methodParams": [
								{
									"param": "code",
									"type": "string",
									"required": "Yes",
									"notes": ""
								},
								{
									"param": "message",
									"type": "string",
									"required": "Yes",
									"notes": ""
								}
							]
						},
						{
							"method": "close",
							"signature": "close([code], [reason])",
							"description": "Close the underlying transport with a WebSocket-style code and reason. Defaults to 1000 (normal closure).",
							"methodParams": [
								{
									"param": "code",
									"type": "number",
									"required": "No",
									"notes": ""
								},
								{
									"param": "reason",
									"type": "string",
									"required": "No",
									"notes": ""
								}
							]
						}
					]
				}
			]
		},
		{
			"name": "SDP Parser",
			"description": "Zero-dependency RFC 8866 Session Description Protocol parser and serializer, with WebRTC-specific attribute extraction (JSEP per RFC 8829: ice-ufrag, ice-pwd, fingerprint, setup, mid, rtcp-mux, direction, rtpmap, fmtp, rid, simulcast, ssrc, extmap, candidate). The parser deliberately does NOT validate semantics that belong to a SignalingHub policy layer (codec allowlists, mDNS blocking, etc.) - it only structures the document.  Policy lives in `lib/webrtc/signaling.js`.",
			"options": [
				{
					"option": "maxBytes",
					"type": "number",
					"default": "65536",
					"notes": "Reject payloads larger than this."
				}
			],
			"params": [
				{
					"param": "text",
					"type": "string",
					"required": "Yes",
					"notes": "The SDP document text."
				}
			],
			"returns": {
				"type": "SessionDescription",
				"description": "Parsed structure."
			},
			"example": "  const { parseSdp } = require('@zero-server/webrtc');\r\n  const desc = parseSdp(offer.sdp);\r\n  console.log(desc.media[0].iceUfrag, desc.media[0].fingerprint);",
			"examples": [
				{
					"code": "  const { parseSdp } = require('@zero-server/webrtc');\r\n  const desc = parseSdp(offer.sdp);\r\n  console.log(desc.media[0].iceUfrag, desc.media[0].fingerprint);",
					"lang": "javascript"
				}
			],
			"methodGroups": [
				{
					"group": "Parser helpers",
					"methods": [
						{
							"method": "stringifySdp",
							"signature": "stringifySdp(session)",
							"description": "Serialize a `SessionDescription` back to RFC 8866 text with CRLF line endings.  The serializer is round-trip safe for documents produced by `parseSdp`: it emits the raw attribute list verbatim so any media-level attribute we did not lift into a structured field is still preserved.",
							"methodParams": [
								{
									"param": "session",
									"type": "SessionDescription",
									"required": "Yes",
									"notes": "Parsed session description."
								}
							],
							"methodReturns": {
								"type": "string",
								"description": "SDP document terminated with CRLF."
							},
							"example": "  const sdp = stringifySdp(parseSdp(offer.sdp));",
							"examples": [
								{
									"code": "  const sdp = stringifySdp(parseSdp(offer.sdp));",
									"lang": "javascript"
								}
							]
						}
					]
				}
			]
		},
		{
			"name": "ICE Candidates",
			"description": "Zero-dependency ICE candidate parser, serializer, and address classifiers (private / loopback / link-local / mDNS), plus a `filterCandidates` helper used by `SignalingHub` to enforce privacy-preserving policies on relayed offers/answers. Candidate grammar follows RFC 8839 §5.1 / RFC 5245 §15.1: candidate-attribute = \"candidate\" \":\" foundation SP component-id SP transport SP priority SP connection-address SP port SP cand-type [SP rel-addr] [SP rel-port] *(SP extension-att-name SP extension-att-value)",
			"params": [
				{
					"param": "line",
					"type": "string",
					"required": "Yes",
					"notes": "Candidate line, e.g.\n`candidate:842163049 1 udp 1677729535 192.168.1.5 50000 typ host`."
				}
			],
			"returns": {
				"type": "IceCandidate",
				"description": "Parsed candidate."
			},
			"example": "  const c = parseCandidate('candidate:1 1 udp 2122194687 1.2.3.4 50001 typ srflx raddr 192.168.1.5 rport 50000');\r\n  if (c.type === 'relay') { console.log('relay candidate'); }",
			"examples": [
				{
					"code": "  const c = parseCandidate('candidate:1 1 udp 2122194687 1.2.3.4 50001 typ srflx raddr 192.168.1.5 rport 50000');\r\n  if (c.type === 'relay') { console.log('relay candidate'); }",
					"lang": "javascript"
				}
			],
			"methodGroups": [
				{
					"group": "ICE & TURN",
					"methods": [
						{
							"method": "stringifyCandidate",
							"signature": "stringifyCandidate(c)",
							"description": "Serialize a parsed candidate back to its canonical line format. Round-trips outputs of `parseCandidate` exactly, including the insertion order of `extensions`.",
							"methodParams": [
								{
									"param": "c",
									"type": "IceCandidate",
									"required": "Yes",
									"notes": "Parsed candidate object."
								}
							],
							"methodReturns": {
								"type": "string",
								"description": "`candidate:...` line (no `a=` prefix)."
							},
							"example": "  const out = stringifyCandidate(parseCandidate(line));",
							"examples": [
								{
									"code": "  const out = stringifyCandidate(parseCandidate(line));",
									"lang": "javascript"
								}
							]
						},
						{
							"method": "isPrivateIp",
							"signature": "isPrivateIp(addr)",
							"description": "True for RFC 1918, RFC 6598 (CGNAT), and IPv6 ULA (RFC 4193) addresses.",
							"methodParams": [
								{
									"param": "addr",
									"type": "string",
									"required": "Yes",
									"notes": "Address to classify."
								}
							],
							"methodReturns": {
								"type": "boolean",
								"description": ""
							}
						},
						{
							"method": "isLoopbackIp",
							"signature": "isLoopbackIp(addr)",
							"description": "True for IPv4 127.0.0.0/8 and IPv6 ::1.",
							"methodParams": [
								{
									"param": "addr",
									"type": "string",
									"required": "Yes",
									"notes": "Address to classify."
								}
							],
							"methodReturns": {
								"type": "boolean",
								"description": ""
							}
						},
						{
							"method": "isLinkLocalIp",
							"signature": "isLinkLocalIp(addr)",
							"description": "True for IPv4 169.254/16 and IPv6 fe80::/10.",
							"methodParams": [
								{
									"param": "addr",
									"type": "string",
									"required": "Yes",
									"notes": "Address to classify."
								}
							],
							"methodReturns": {
								"type": "boolean",
								"description": ""
							}
						},
						{
							"method": "isMdnsHostname",
							"signature": "isMdnsHostname(host)",
							"description": "True for mDNS `.local` hostnames used by browsers to avoid leaking local IPs (Chrome's mDNS ICE candidates - RFC 8624 / draft-ietf-mmusic-mdns-ice-candidates).",
							"methodParams": [
								{
									"param": "host",
									"type": "string",
									"required": "Yes",
									"notes": "Hostname to test."
								}
							],
							"methodReturns": {
								"type": "boolean",
								"description": ""
							}
						},
						{
							"method": "filterCandidates",
							"signature": "filterCandidates(candidates, [policy])",
							"description": "Filter an array of candidates (lines or parsed objects) against a policy. Returns the same shape it was given: if you pass strings you get strings back; if you pass parsed objects you get parsed objects back.  Unparseable string lines are silently skipped so a single bad candidate never poisons the whole offer.",
							"methodParams": [
								{
									"param": "candidates",
									"type": "Array<string | IceCandidate>",
									"required": "Yes",
									"notes": "Input list."
								},
								{
									"param": "policy",
									"type": "CandidateFilterPolicy",
									"required": "No",
									"notes": "Policy (all defaults are permissive)."
								}
							],
							"methodReturns": {
								"type": "Array<string | IceCandidate>",
								"description": "Surviving candidates, same element shape as input."
							},
							"example": "  const safe = filterCandidates(offer.candidates, {\r\n      blockPrivate: true,\r\n      blockMdns:    true,\r\n      allowedTypes: ['srflx', 'relay'],\r\n  });",
							"examples": [
								{
									"code": "  const safe = filterCandidates(offer.candidates, {\r\n      blockPrivate: true,\r\n      blockMdns:    true,\r\n      allowedTypes: ['srflx', 'relay'],\r\n  });",
									"lang": "javascript"
								}
							]
						}
					]
				}
			]
		},
		{
			"name": "STUN Client",
			"description": "Zero-dependency RFC 8489 STUN (Session Traversal Utilities for NAT) client.  Sends a Binding Request over UDP, parses the Binding Response, and returns the server-reflexive address decoded from XOR-MAPPED-ADDRESS (or, as a fallback, the deprecated MAPPED-ADDRESS attribute used by some legacy servers). Only the parts of RFC 8489 we need for NAT discovery are implemented: BINDING method, REQUEST and SUCCESS classes, XOR-MAPPED-ADDRESS / MAPPED-ADDRESS attributes for IPv4 + IPv6. Authenticated TURN allocations are handled in `lib/webrtc/turn/`.",
			"params": [
				{
					"param": "transactionId",
					"type": "Buffer",
					"required": "No",
					"notes": "12-byte ID; one is generated if omitted."
				}
			],
			"returns": {
				"type": "{buffer:Buffer, transactionId:Buffer}",
				"description": ""
			},
			"methodGroups": [
				{
					"group": "ICE & TURN",
					"methods": [
						{
							"method": "stunBinding",
							"signature": "stunBinding(opts)",
							"description": "Discover the public (server-reflexive) address by sending a STUN Binding Request to `opts.host:opts.port`. Retries with the same transaction ID until a matching Binding Success is received or `retries` attempts elapse.  Closes the socket on resolve/reject.",
							"methodParams": [
								{
									"param": "opts",
									"type": "StunBindingOptions",
									"required": "Yes",
									"notes": ""
								}
							],
							"methodReturns": {
								"type": "Promise<StunBindingResult>",
								"description": ""
							},
							"example": "  const { address, port } = await stunBinding({ host: 'stun.l.google.com', port: 19302 });",
							"examples": [
								{
									"code": "  const { address, port } = await stunBinding({ host: 'stun.l.google.com', port: 19302 });",
									"lang": "javascript"
								}
							]
						},
						{
							"method": "decodeMessage",
							"signature": "decodeMessage(buf)",
							"description": "Parse a STUN message (header + attributes).",
							"methodParams": [
								{
									"param": "buf",
									"type": "Buffer",
									"required": "Yes",
									"notes": "Raw datagram."
								}
							],
							"methodReturns": {
								"type": "{method:number, class:number, transactionId:Buffer, attributes:Array<{type:number,value:Buffer}>}",
								"description": ""
							}
						},
						{
							"method": "encodeXorMappedAddress",
							"signature": "encodeXorMappedAddress(address, port, transactionId)",
							"description": "Encode an XOR-MAPPED-ADDRESS attribute body (no TLV header).",
							"methodParams": [
								{
									"param": "address",
									"type": "string",
									"required": "Yes",
									"notes": "IPv4 or IPv6 string."
								},
								{
									"param": "port",
									"type": "number",
									"required": "Yes",
									"notes": "0-65535."
								},
								{
									"param": "transactionId",
									"type": "Buffer",
									"required": "Yes",
									"notes": "12-byte transaction ID (needed for IPv6)."
								}
							],
							"methodReturns": {
								"type": "Buffer",
								"description": ""
							}
						},
						{
							"method": "decodeXorMappedAddress",
							"signature": "decodeXorMappedAddress(buf, transactionId)",
							"description": "Decode an XOR-MAPPED-ADDRESS attribute body to `{family, address, port}`.",
							"methodParams": [
								{
									"param": "buf",
									"type": "Buffer",
									"required": "Yes",
									"notes": "Attribute value bytes."
								},
								{
									"param": "transactionId",
									"type": "Buffer",
									"required": "Yes",
									"notes": "12-byte ID from the response header."
								}
							],
							"methodReturns": {
								"type": "{family:number, address:string, port:number}",
								"description": ""
							}
						}
					]
				}
			]
		},
		{
			"name": "TURN Credentials",
			"description": "RFC 7635 ephemeral TURN credentials. Generates time-limited username / credential pairs that any RFC 7635-compatible TURN server (notably `coturn` with `use-auth-secret` + `static-auth-secret=<S>`) will accept. Wire format (RFC 7635 §6.2): username   = \"<unix-expiry>:<userId>\" credential = base64( HMAC-SHA1( <secret>, username ) ) The returned object is shaped like an `RTCIceServer` entry so it can be embedded straight into the ICE-server list a signaling endpoint serves to browsers.",
			"returns": {
				"type": "TurnCredentials",
				"description": ""
			},
			"example": "  const creds = issueTurnCredentials({\r\n      secret:  process.env.TURN_SHARED_SECRET,\r\n      userId:  req.user.id,\r\n      ttl:     '20m',\r\n      servers: ['turn:turn.example.com:3478?transport=udp'],\r\n  });\r\n  res.json(creds);",
			"examples": [
				{
					"code": "  const creds = issueTurnCredentials({\r\n      secret:  process.env.TURN_SHARED_SECRET,\r\n      userId:  req.user.id,\r\n      ttl:     '20m',\r\n      servers: ['turn:turn.example.com:3478?transport=udp'],\r\n  });\r\n  res.json(creds);",
					"lang": "javascript"
				}
			]
		},
		{
			"name": "TURN Server",
			"description": "Zero-dependency embedded TURN server (RFC 5766) backing the `TurnServer` public API.  Implements the long-term-credential auth flow paired with `issueTurnCredentials` ephemeral accounts, UDP allocations, permissions, Send / Data indications, channel bindings, lifetimes, and per-user quotas. The hot path is intentionally compact: all framing lives in `lib/webrtc/turn/codec.js` and the server only owns state + relay-socket multiplexing.  TCP and TLS listeners are reserved in the constructor surface and will be implemented in a later PR; calling `start()` against either currently throws `TURN_TRANSPORT_UNSUPPORTED`.",
			"example": "  const { TurnServer, issueTurnCredentials } = require('@zero-server/webrtc');\r\n\n  const turn = new TurnServer({\r\n      secret:   process.env.TURN_SECRET,\r\n      realm:    'rtc.example.com',\r\n      listeners: [{ proto: 'udp', port: 3478 }],\r\n      quotas:   { maxAllocationsPerUser: 4, maxBytesPerMinute: 50_000_000 },\r\n  });\r\n  await turn.start();\r\n\n  const creds = issueTurnCredentials({\r\n      secret:  process.env.TURN_SECRET,\r\n      userId:  req.user.id,\r\n      servers: ['turn:rtc.example.com:3478'],\r\n      ttl:     '20m',\r\n  });\r\n  res.json(creds);",
			"examples": [
				{
					"code": "  const { TurnServer, issueTurnCredentials } = require('@zero-server/webrtc');\r\n\n  const turn = new TurnServer({\r\n      secret:   process.env.TURN_SECRET,\r\n      realm:    'rtc.example.com',\r\n      listeners: [{ proto: 'udp', port: 3478 }],\r\n      quotas:   { maxAllocationsPerUser: 4, maxBytesPerMinute: 50_000_000 },\r\n  });\r\n  await turn.start();\r\n\n  const creds = issueTurnCredentials({\r\n      secret:  process.env.TURN_SECRET,\r\n      userId:  req.user.id,\r\n      servers: ['turn:rtc.example.com:3478'],\r\n      ttl:     '20m',\r\n  });\r\n  res.json(creds);",
					"lang": "javascript"
				}
			],
			"options": [
				{
					"option": "secret",
					"type": "string",
					"default": "-",
					"notes": "\nShared HMAC secret for ephemeral credentials produced by\n{@link module:webrtc/turn/credentials.issueTurnCredentials}."
				},
				{
					"option": "realm",
					"type": "string",
					"default": "'zero-server'",
					"notes": ""
				},
				{
					"option": "listeners",
					"type": "Array<{proto:'udp' | 'tcp' | 'tls',port:number,host?:string,tls?:object}>",
					"default": "-",
					"notes": ""
				},
				{
					"option": "quotas",
					"type": "object",
					"default": "-",
					"notes": ""
				},
				{
					"option": "quotas.maxAllocationsPerUser",
					"type": "number",
					"default": "Infinity",
					"notes": ""
				},
				{
					"option": "quotas.maxBytesPerMinute",
					"type": "number",
					"default": "Infinity",
					"notes": ""
				},
				{
					"option": "defaultLifetime",
					"type": "number",
					"default": "600",
					"notes": ""
				},
				{
					"option": "maxLifetime",
					"type": "number",
					"default": "3600",
					"notes": ""
				},
				{
					"option": "relayHost",
					"type": "string",
					"default": "'127.0.0.1'",
					"notes": "\nAddress bound by each per-allocation relay socket.  Production\ndeployments should pass the server's public IP."
				}
			],
			"methodGroups": [
				{
					"group": "Methods",
					"methods": [
						{
							"method": "start",
							"signature": "start()",
							"description": "Bind all configured listeners.  Resolves once every listener is listening; rejects on the first bind error.",
							"methodReturns": {
								"type": "Promise<void>",
								"description": ""
							}
						},
						{
							"method": "stop",
							"signature": "stop()",
							"description": "Close all listeners and free every allocation's relay socket.",
							"methodReturns": {
								"type": "Promise<void>",
								"description": ""
							}
						},
						{
							"method": "address",
							"signature": "address()",
							"description": "The bound address of the first listener (handy for tests that ask the kernel for an ephemeral port via `port: 0`).",
							"methodReturns": {
								"type": "{address:string, port:number} | null",
								"description": ""
							}
						}
					]
				}
			]
		},
		{
			"name": "SFU Adapter",
			"description": "SFU adapter base interface and discovery loader. `SfuAdapter` defines the contract every backend (memory / mediasoup / livekit / custom) must implement.  `loadSfuAdapter()` resolves either a pre-constructed instance, a known name ('memory', 'mediasoup', 'livekit'), or a duck-typed object into a concrete adapter, throwing `WEBRTC_SFU_NOT_INSTALLED` when a native peerDep is missing.",
			"methodGroups": [
				{
					"group": "Methods",
					"methods": [
						{
							"method": "createRouter",
							"signature": "createRouter()",
							"description": "Override to create a routing context for a single room."
						},
						{
							"method": "createTransport",
							"signature": "createTransport()",
							"description": "Override to allocate a WebRTC transport for a peer in a router."
						},
						{
							"method": "produce",
							"signature": "produce()",
							"description": "Override to bind a producer ('audio' | 'video') to a transport."
						},
						{
							"method": "consume",
							"signature": "consume()",
							"description": "Override to bind a consumer of `producerId` to a transport."
						},
						{
							"method": "pauseProducer",
							"signature": "pauseProducer()",
							"description": "Override to pause a producer (mute upstream forwarding)."
						},
						{
							"method": "resumeProducer",
							"signature": "resumeProducer()",
							"description": "Override to resume a previously paused producer."
						},
						{
							"method": "closeRouter",
							"signature": "closeRouter()",
							"description": "Override to close a router and cascade-close its transports."
						},
						{
							"method": "stats",
							"signature": "stats()",
							"description": "Override to return adapter stats; `scope` may be a routerId/transportId."
						},
						{
							"method": "onEvent",
							"signature": "onEvent()",
							"description": "Register a handler invoked as `(event, payload)` for adapter-level events ('producer-new', 'producer-pause', 'consumer-new', 'transport-close', 'router-close', etc.). Returns an unsubscribe function."
						},
						{
							"method": "loadSfuAdapter",
							"signature": "loadSfuAdapter(spec, [opts])",
							"description": "Lazy-load and instantiate an SFU adapter.",
							"methodParams": [
								{
									"param": "spec",
									"type": "object | string",
									"required": "Yes",
									"notes": "one of:\n- an object exposing the SfuAdapter contract (returned as-is),\n- 'memory' | 'mediasoup' | 'livekit' | adapter package id."
								},
								{
									"param": "opts",
									"type": "object",
									"required": "No",
									"notes": "constructor options forwarded to the adapter."
								}
							],
							"methodReturns": {
								"type": "SfuAdapter",
								"description": ""
							}
						}
					]
				}
			]
		},
		{
			"name": "Memory SFU Adapter",
			"description": "In-process \"memory\" SFU adapter. A passthrough router that never touches the network: every produce() call records a logical producer, every consume() call records a logical consumer, and events are emitted via {@link SfuAdapter#onEvent}. Perfect for unit tests, ≤ 4-peer audio-only rooms, and local dev where the cost of running mediasoup or LiveKit is unjustified. The adapter does NOT decode or forward media packets - it models bookkeeping only.  Real packet forwarding lives in native adapters (mediasoup, LiveKit)."
		},
		{
			"name": "Mediasoup SFU Adapter",
			"description": "mediasoup-backed SFU adapter (peerDependency on `mediasoup`). Wraps a single mediasoup `Worker` and one `Router` per createRouter() call.  WebRTC transports are created with `router.createWebRtcTransport()` and produce / consume / pause / resume / close / stats all delegate to the native mediasoup objects. `mediasoup` is loaded lazily.  Tests inject a stub via `opts.mediasoup`; in production the constructor `require('mediasoup')`s the real package and throws `WEBRTC_SFU_NOT_INSTALLED` if it is missing.",
			"options": [
				{
					"option": "mediasoup",
					"type": "object",
					"default": "-",
					"notes": "Injected mediasoup module (testing); defaults to `require('mediasoup')`."
				},
				{
					"option": "worker",
					"type": "object",
					"default": "-",
					"notes": "Pre-created `mediasoup.Worker`; bypasses the lazy worker bootstrap."
				},
				{
					"option": "workerSettings",
					"type": "object",
					"default": "-",
					"notes": "Forwarded to `mediasoup.createWorker(...)`."
				},
				{
					"option": "mediaCodecs",
					"type": "Array",
					"default": "-",
					"notes": "Default router media codecs."
				},
				{
					"option": "webRtcTransportOptions",
					"type": "object",
					"default": "-",
					"notes": "Default `router.createWebRtcTransport(...)` options."
				}
			],
			"methodGroups": [
				{
					"group": "Methods",
					"methods": [
						{
							"method": "close",
							"signature": "close()",
							"description": "Best-effort shutdown: closes every router, then the worker if we own it."
						}
					]
				}
			]
		},
		{
			"name": "LiveKit SFU Adapter",
			"description": "LiveKit-backed SFU adapter (peerDependency on `livekit-server-sdk`). LiveKit's media plane is controlled remotely: rooms live on the LiveKit server, participants connect directly with a signed JWT, and the server SDK exposes a control-plane REST API.  This adapter maps the {@link SfuAdapter} contract onto that model: - createRouter(opts)          -> RoomServiceClient.createRoom(...) - createTransport(router,peer) -> mints an AccessToken (the \"transport\" handle is the URL + JWT the peer uses to connect to LiveKit directly) - produce / consume           -> local bookkeeping; LiveKit handles the actual media plane client-side - pauseProducer / resume      -> RoomServiceClient.mutePublishedTrack() when the producer was registered with a `{room, identity, trackSid}` hint; otherwise emits the event without touching the server - closeRouter(routerId)       -> RoomServiceClient.deleteRoom(...) - stats()                     -> RoomServiceClient.listRooms() / listParticipants(...) plus local counters `livekit-server-sdk` is loaded lazily.  Tests inject a stub via `opts.livekit`; in production the constructor `require`s the package and throws `WEBRTC_SFU_NOT_INSTALLED` if it is missing.",
			"options": [
				{
					"option": "url",
					"type": "string",
					"default": "-",
					"notes": "LiveKit server URL (wss://...)."
				},
				{
					"option": "apiKey",
					"type": "string",
					"default": "-",
					"notes": "LiveKit API key."
				},
				{
					"option": "apiSecret",
					"type": "string",
					"default": "-",
					"notes": "LiveKit API secret."
				},
				{
					"option": "livekit",
					"type": "object",
					"default": "-",
					"notes": "Injected `livekit-server-sdk` module (testing)."
				},
				{
					"option": "client",
					"type": "object",
					"default": "-",
					"notes": "Pre-built `RoomServiceClient` (testing)."
				},
				{
					"option": "defaultRoomOpts",
					"type": "object",
					"default": "-",
					"notes": "Forwarded to `createRoom()` when fields are missing."
				},
				{
					"option": "defaultGrants",
					"type": "object",
					"default": "-",
					"notes": "Default `{canPublish, canSubscribe, ...}` for minted tokens."
				},
				{
					"option": "tokenTtl",
					"type": "string",
					"default": "'1h'",
					"notes": "AccessToken TTL."
				}
			],
			"methodGroups": [
				{
					"group": "Methods",
					"methods": [
						{
							"method": "createRouter",
							"signature": "createRouter()",
							"description": "Create a LiveKit room.  `opts.name` overrides the auto-generated name. Returns a router handle whose `id` is the room name."
						},
						{
							"method": "createTransport",
							"signature": "createTransport()",
							"description": "Mint an AccessToken for `peer` to join the LiveKit room.  Returns a transport handle containing the JWT and URL the peer hands to the LiveKit client SDK."
						}
					]
				}
			]
		},
		{
			"name": "Bot Peer (wrtc)",
			"description": "Server-side WebRTC peer (\"bot\") built on the `wrtc` peerDependency. `spawnBotPeer({hub, room, ...})` attaches an in-process peer to a {@link SignalingHub}, joins a room, and drives a real {@link RTCPeerConnection} per remote peer (using the Node.js `wrtc` binding).  It implements the standard JSEP perfect-negotiation pattern and is designed for headless workloads such as recording, transcription, AI agents, and SFU verification harnesses. The `wrtc` peerDependency is loaded lazily; in production any of `wrtc` or `@roamhq/wrtc` is acceptable.  Tests inject a fake via `opts.wrtc`.",
			"methodGroups": [
				{
					"group": "Methods",
					"methods": [
						{
							"method": "spawnBotPeer",
							"signature": "spawnBotPeer(opts)",
							"description": "Spawn a server-side bot peer that joins `room` on the given hub.",
							"methodParams": [
								{
									"param": "opts",
									"type": "object",
									"required": "Yes",
									"notes": ""
								}
							],
							"methodOptions": [
								{
									"option": "hub",
									"type": "object",
									"default": "-",
									"notes": "The {@link SignalingHub} instance."
								},
								{
									"option": "room",
									"type": "string",
									"default": "-",
									"notes": "Room name to join."
								},
								{
									"option": "user",
									"type": "*",
									"default": "-",
									"notes": "Opaque user object attached to the peer."
								},
								{
									"option": "ip",
									"type": "string",
									"default": "'127.0.0.1'",
									"notes": "IP recorded on the attached peer."
								},
								{
									"option": "joinToken",
									"type": "string",
									"default": "-",
									"notes": "Optional join token forwarded to the hub."
								},
								{
									"option": "iceServers",
									"type": "Array",
									"default": "[",
									"notes": "]    RTCConfiguration.iceServers."
								},
								{
									"option": "rtcConfig",
									"type": "object",
									"default": "-",
									"notes": "Additional RTCConfiguration fields."
								},
								{
									"option": "wrtc",
									"type": "object",
									"default": "-",
									"notes": "Injected `wrtc` module (testing)."
								},
								{
									"option": "onTrack",
									"type": "Function",
									"default": "-",
									"notes": "(track, streams, fromPeerId) => void"
								},
								{
									"option": "onDataChannel",
									"type": "Function",
									"default": "-",
									"notes": "(channel, fromPeerId) => void"
								},
								{
									"option": "onPeerJoin",
									"type": "Function",
									"default": "-",
									"notes": "(remotePeerId) => void"
								},
								{
									"option": "onPeerLeave",
									"type": "Function",
									"default": "-",
									"notes": "(remotePeerId) => void"
								},
								{
									"option": "onError",
									"type": "Function",
									"default": "-",
									"notes": "(err) => void  (non-fatal errors)"
								}
							]
						}
					]
				}
			]
		},
		{
			"name": "Join Tokens",
			"description": "Signed, short-TTL join tokens that authenticate a peer's right to join a specific room.  JWT-shaped (HS256 by default) and audience-scoped to `room:<name>` so a token leaked from one channel cannot be replayed against another. Reuses the canonical sign/verify primitives from `lib/auth/jwt.js`.",
			"options": [
				{
					"option": "secret",
					"type": "string | Buffer",
					"default": "-",
					"notes": "HMAC secret (HS256) or PEM key (RS256)."
				},
				{
					"option": "user",
					"type": "string | object",
					"default": "-",
					"notes": "User identifier (string) or object containing `id`."
				},
				{
					"option": "room",
					"type": "string",
					"default": "-",
					"notes": "Target room name."
				},
				{
					"option": "ttl",
					"type": "number",
					"default": "300",
					"notes": "Seconds until expiry.  Negative values are accepted\n(used by tests to mint already-expired tokens)."
				},
				{
					"option": "algorithm",
					"type": "string",
					"default": "'HS256'",
					"notes": ""
				},
				{
					"option": "audience",
					"type": "string | string[]",
					"default": "-",
					"notes": "Override the default `room:<name>` audience."
				},
				{
					"option": "claims",
					"type": "object",
					"default": "-",
					"notes": "Additional claims merged into the payload."
				}
			],
			"returns": {
				"type": "string",
				"description": "Compact JWT."
			},
			"example": "  const token = signJoinToken({\r\n      secret: process.env.JOIN_SECRET,\r\n      user:   req.user,\r\n      room:   'boardroom',\r\n      ttl:    300,\r\n  });\r\n  res.json({ wsUrl: '/rtc', token });",
			"examples": [
				{
					"code": "  const token = signJoinToken({\r\n      secret: process.env.JOIN_SECRET,\r\n      user:   req.user,\r\n      room:   'boardroom',\r\n      ttl:    300,\r\n  });\r\n  res.json({ wsUrl: '/rtc', token });",
					"lang": "javascript"
				}
			],
			"methodGroups": [
				{
					"group": "Signaling",
					"methods": [
						{
							"method": "verifyJoinToken",
							"signature": "verifyJoinToken(token, opts)",
							"description": "Verify a join token and return its payload.  Throws a `WebRTCError` with `code: 'INVALID_TOKEN'` on any failure - bad signature, expired, audience mismatch, malformed, etc.",
							"methodParams": [
								{
									"param": "token",
									"type": "string",
									"required": "Yes",
									"notes": ""
								},
								{
									"param": "opts",
									"type": "object",
									"required": "Yes",
									"notes": ""
								}
							],
							"methodOptions": [
								{
									"option": "secret",
									"type": "string | Buffer",
									"default": "-",
									"notes": ""
								},
								{
									"option": "room",
									"type": "string",
									"default": "-",
									"notes": "If supplied, audience must be `room:<room>`."
								},
								{
									"option": "audience",
									"type": "string | string[]",
									"default": "-",
									"notes": "Explicit audience override."
								},
								{
									"option": "algorithms",
									"type": "string | string[]",
									"default": "['HS256'",
									"notes": "]"
								},
								{
									"option": "clockTolerance",
									"type": "number",
									"default": "0",
									"notes": ""
								}
							],
							"methodReturns": {
								"type": "object",
								"description": "Verified payload."
							}
						}
					]
				}
			]
		},
		{
			"name": "Observability",
			"description": "Optional metrics + tracing wiring for a {@link SignalingHub}. Pass a `MetricsRegistry`, a `Tracer`, or both; the binder subscribes to the hub's lifecycle events and exports the six standard `zs_webrtc_*` Prometheus series plus per-operation OpenTelemetry-compatible spans.",
			"options": [
				{
					"option": "metrics",
					"type": "MetricsRegistry",
					"default": "-",
					"notes": "Prometheus-compatible registry."
				},
				{
					"option": "tracer",
					"type": "Tracer",
					"default": "-",
					"notes": "Tracer for span emission."
				}
			],
			"params": [
				{
					"param": "hub",
					"type": "SignalingHub",
					"required": "Yes",
					"notes": "The hub to instrument."
				}
			],
			"returns": {
				"type": "SignalingHub",
				"description": "The same hub, for chaining."
			},
			"example": "  const hub = new SignalingHub();\r\n  bindObservability(hub, { metrics: app.metrics() });",
			"examples": [
				{
					"code": "  const hub = new SignalingHub();\r\n  bindObservability(hub, { metrics: app.metrics() });",
					"lang": "javascript",
					"title": "Plug into an existing app"
				}
			]
		},
		{
			"name": "E2EE Key Relay",
			"description": "End-to-end-encrypted key relay channel for WebRTC. The hub never sees plaintext SFrame / Insertable-Streams keys. Publishers wrap each rotation in a sealed envelope (X25519 ECDH + HKDF-SHA-256 + AES-256-GCM) and broadcast it via the `e2ee-key` wire message; subscribers in the same room receive the sealed payload and decrypt locally with their private key. For deployments that use a different sealing primitive (NaCl `crypto_box_seal`, libsignal, etc.) the {@link E2eeChannel} works with any opaque `Buffer` - the {@link sealKey} / {@link openSealedKey} helpers are provided as a zero-dependency default that satisfies the HIPAA / FINRA \"server is opaque\" requirement.",
			"params": [
				{
					"param": "peer",
					"type": "Peer",
					"required": "Yes",
					"notes": ""
				},
				{
					"param": "hub",
					"type": "SignalingHub",
					"required": "Yes",
					"notes": ""
				}
			],
			"methodGroups": [
				{
					"group": "Public crypto helpers",
					"methods": [
						{
							"method": "generateE2eeKeyPair",
							"signature": "generateE2eeKeyPair()",
							"description": "Generate a fresh X25519 keypair suitable for {@link sealKey} / {@link openSealedKey}.",
							"methodReturns": {
								"type": "{publicKey: KeyObject, privateKey: KeyObject}",
								"description": ""
							},
							"example": "  const { publicKey, privateKey } = generateE2eeKeyPair();\r\n  const wireKey = publicKey.export({ format: 'jwk' }).x; // base64url",
							"examples": [
								{
									"code": "  const { publicKey, privateKey } = generateE2eeKeyPair();\r\n  const wireKey = publicKey.export({ format: 'jwk' }).x; // base64url",
									"lang": "javascript"
								}
							]
						},
						{
							"method": "sealKey",
							"signature": "sealKey(plaintext, recipientPubKey)",
							"description": "Seal an opaque byte string for a single recipient using the project's default envelope (X25519 ECDH + HKDF-SHA-256 + AES-256-GCM). Envelope layout: `[ver:1] [ephPubRaw:32] [nonce:12] [ciphertext:N] [tag:16]`",
							"methodParams": [
								{
									"param": "plaintext",
									"type": "Buffer | Uint8Array",
									"required": "Yes",
									"notes": "The bytes to encrypt."
								},
								{
									"param": "recipientPubKey",
									"type": "KeyObject | Buffer",
									"required": "Yes",
									"notes": "Recipient's X25519 public\nkey (KeyObject or 32-byte raw)."
								}
							],
							"methodReturns": {
								"type": "Buffer",
								"description": "The sealed envelope."
							},
							"example": "  const sealed = sealKey(Buffer.from(sframeKey), bob.publicKey);\r\n  peer.e2ee.publish(epoch, sealed);",
							"examples": [
								{
									"code": "  const sealed = sealKey(Buffer.from(sframeKey), bob.publicKey);\r\n  peer.e2ee.publish(epoch, sealed);",
									"lang": "javascript"
								}
							]
						},
						{
							"method": "openSealedKey",
							"signature": "openSealedKey(sealed, recipientPrivKey)",
							"description": "Open an envelope produced by {@link sealKey} with the recipient's private key.  Throws if the envelope is malformed, was sealed for a different recipient, or was tampered with in flight.",
							"methodParams": [
								{
									"param": "sealed",
									"type": "Buffer | Uint8Array",
									"required": "Yes",
									"notes": "Sealed envelope."
								},
								{
									"param": "recipientPrivKey",
									"type": "KeyObject | Buffer",
									"required": "Yes",
									"notes": "Recipient's X25519 private key."
								}
							],
							"methodReturns": {
								"type": "Buffer",
								"description": "Decrypted plaintext."
							},
							"example": "  const sframeKey = openSealedKey(received.key, bob.privateKey);",
							"examples": [
								{
									"code": "  const sframeKey = openSealedKey(received.key, bob.privateKey);",
									"lang": "javascript"
								}
							]
						}
					]
				},
				{
					"group": "E2EE",
					"methods": [
						{
							"method": "publish",
							"signature": "publish(epoch, key)",
							"description": "Broadcast a sealed key to the rest of the peer's room.",
							"methodParams": [
								{
									"param": "epoch",
									"type": "number | null",
									"required": "Yes",
									"notes": "Explicit epoch, or `null` to auto-increment."
								},
								{
									"param": "key",
									"type": "Buffer | Uint8Array | string",
									"required": "Yes",
									"notes": "Sealed bytes (or wire-ready string)."
								}
							],
							"methodReturns": {
								"type": "number",
								"description": "The epoch the key was published under."
							},
							"example": "  const sealed = sealKey(sframeKey, bob.publicKey);\r\n  const epoch  = peer.e2ee.publish(null, sealed);",
							"examples": [
								{
									"code": "  const sealed = sealKey(sframeKey, bob.publicKey);\r\n  const epoch  = peer.e2ee.publish(null, sealed);",
									"lang": "javascript"
								}
							]
						},
						{
							"method": "subscribe",
							"signature": "subscribe(fn)",
							"description": "Receive sealed keys published by *other* peers in the same room.",
							"methodParams": [
								{
									"param": "fn",
									"type": "(ev: {from: string, epoch: number, key: Buffer}) => void",
									"required": "Yes",
									"notes": ""
								}
							],
							"methodReturns": {
								"type": "() => void",
								"description": "Unsubscribe function."
							},
							"example": "  peer.e2ee.subscribe(({ from, epoch, key }) => {\r\n      const sframeKey = openSealedKey(key, myPrivKey);\r\n      sframeContext.setKey(epoch, sframeKey);\r\n  });",
							"examples": [
								{
									"code": "  peer.e2ee.subscribe(({ from, epoch, key }) => {\r\n      const sframeKey = openSealedKey(key, myPrivKey);\r\n      sframeContext.setKey(epoch, sframeKey);\r\n  });",
									"lang": "javascript"
								}
							]
						},
						{
							"method": "attachE2ee",
							"signature": "attachE2ee(peer, hub)",
							"description": "Install an {@link E2eeChannel} on a peer as `peer.e2ee`.  Idempotent: a second call returns the existing channel.",
							"methodParams": [
								{
									"param": "peer",
									"type": "Peer",
									"required": "Yes",
									"notes": ""
								},
								{
									"param": "hub",
									"type": "SignalingHub",
									"required": "Yes",
									"notes": ""
								}
							],
							"methodReturns": {
								"type": "E2eeChannel",
								"description": ""
							},
							"example": "  hub.on('join', ({ peer }) => attachE2ee(peer, hub));",
							"examples": [
								{
									"code": "  hub.on('join', ({ peer }) => attachE2ee(peer, hub));",
									"lang": "javascript",
									"title": "Attach E2EE on every join"
								}
							]
						}
					]
				}
			]
		},
		{
			"name": "Cluster Adapter",
			"description": "Cluster adapter for the WebRTC signaling hub. `useCluster(hub, adapter)` glues a `SignalingHub` to any pub/sub adapter that implements `{ publish(channel, message), subscribe( channel, cb) -> unsubscribe }`.  Once attached: - Every local `join` / `leave` is announced cluster-wide so other nodes can resolve a `peer.id` to its owning node. - Every `room.broadcast(...)` is mirrored to peers in the same room on other nodes. - Direct frames (`offer`, `answer`, `ice`) addressed to a peer that lives on a different node are forwarded to that node's inbox. The adapter itself is intentionally tiny so production deployments can wire it up to Redis, NATS, Kafka, or any in-house bus.  A `MemoryClusterAdapter` is provided for tests and single-process simulations.",
			"options": [
				{
					"option": "nodeId",
					"type": "string",
					"default": "-",
					"notes": "Stable id for this node.  Defaults to\na random 8-byte hex string."
				}
			],
			"params": [
				{
					"param": "hub",
					"type": "import('./signaling').SignalingHub",
					"required": "Yes",
					"notes": ""
				},
				{
					"param": "adapter",
					"type": "{publish:Function, subscribe:Function}",
					"required": "Yes",
					"notes": ""
				}
			],
			"methodGroups": [
				{
					"group": "Cluster",
					"methods": [
						{
							"method": "locate",
							"signature": "locate(peerId)",
							"description": "Look up the node that owns a remote peer, if any.",
							"methodParams": [
								{
									"param": "peerId",
									"type": "string",
									"required": "Yes",
									"notes": ""
								}
							],
							"methodReturns": {
								"type": "{nodeId:string, room:string} | null",
								"description": ""
							}
						},
						{
							"method": "routeDirect",
							"signature": "routeDirect(toPeerId, type, payload)",
							"description": "Forward a direct frame to a peer on another node.  Called by the hub when a routed message (`offer` / `answer` / `ice`) targets a peer id that is not in the local registry.",
							"methodParams": [
								{
									"param": "toPeerId",
									"type": "string",
									"required": "Yes",
									"notes": ""
								},
								{
									"param": "type",
									"type": "string",
									"required": "Yes",
									"notes": ""
								},
								{
									"param": "payload",
									"type": "object",
									"required": "Yes",
									"notes": ""
								}
							],
							"methodReturns": {
								"type": "boolean",
								"description": "`true` if a remote node was addressed."
							}
						},
						{
							"method": "fanoutRoom",
							"signature": "fanoutRoom(roomName, type, payload, [excludeId])",
							"description": "Mirror a `room.broadcast(...)` to peers in the same room on other nodes.  Called automatically from `Room#broadcast`.",
							"methodParams": [
								{
									"param": "roomName",
									"type": "string",
									"required": "Yes",
									"notes": ""
								},
								{
									"param": "type",
									"type": "string",
									"required": "Yes",
									"notes": ""
								},
								{
									"param": "payload",
									"type": "object",
									"required": "Yes",
									"notes": ""
								},
								{
									"param": "excludeId",
									"type": "string",
									"required": "No",
									"notes": ""
								}
							]
						},
						{
							"method": "close",
							"signature": "close()",
							"description": "Tear down all subscriptions and clear remote state."
						},
						{
							"method": "publish",
							"signature": "publish(channel, message)",
							"methodParams": [
								{
									"param": "channel",
									"type": "string",
									"required": "Yes",
									"notes": ""
								},
								{
									"param": "message",
									"type": "*",
									"required": "Yes",
									"notes": ""
								}
							]
						},
						{
							"method": "subscribe",
							"signature": "subscribe(channel, fn)",
							"methodParams": [
								{
									"param": "channel",
									"type": "string",
									"required": "Yes",
									"notes": ""
								},
								{
									"param": "fn",
									"type": "(msg:*) => void",
									"required": "Yes",
									"notes": ""
								}
							],
							"methodReturns": {
								"type": "() => void",
								"description": "unsubscribe"
							}
						}
					]
				},
				{
					"group": "useCluster",
					"methods": [
						{
							"method": "useCluster",
							"signature": "useCluster(hub, adapter, [opts])",
							"description": "Attach a cluster adapter to a `SignalingHub`.",
							"methodParams": [
								{
									"param": "hub",
									"type": "import('./signaling').SignalingHub",
									"required": "Yes",
									"notes": ""
								},
								{
									"param": "adapter",
									"type": "{publish:Function, subscribe:Function}",
									"required": "Yes",
									"notes": ""
								},
								{
									"param": "opts",
									"type": "object",
									"required": "No",
									"notes": ""
								}
							],
							"methodOptions": [
								{
									"option": "nodeId",
									"type": "string",
									"default": "-",
									"notes": ""
								}
							],
							"methodReturns": {
								"type": "ClusterCoordinator",
								"description": ""
							},
							"example": "  const { SignalingHub, useCluster, MemoryClusterAdapter } = require('@zero-server/webrtc');\r\n  const adapter = new MemoryClusterAdapter();\r\n  const a = new SignalingHub(); useCluster(a, adapter, { nodeId: 'a' });\r\n  const b = new SignalingHub(); useCluster(b, adapter, { nodeId: 'b' });",
							"examples": [
								{
									"code": "  const { SignalingHub, useCluster, MemoryClusterAdapter } = require('@zero-server/webrtc');\r\n  const adapter = new MemoryClusterAdapter();\r\n  const a = new SignalingHub(); useCluster(a, adapter, { nodeId: 'a' });\r\n  const b = new SignalingHub(); useCluster(b, adapter, { nodeId: 'b' });",
									"lang": "javascript",
									"title": "In-memory cluster (tests / single-process simulation)"
								},
								{
									"code": "  const Redis = require('ioredis');\r\n  const pub = new Redis(), sub = new Redis();\r\n  const adapter = {\r\n      publish:   (ch, msg) => pub.publish(ch, JSON.stringify(msg)),\r\n      subscribe: (ch, cb) => {\r\n          sub.subscribe(ch);\r\n          const on = (c, raw) => { if (c === ch) cb(JSON.parse(raw)); };\r\n          sub.on('message', on);\r\n          return () => { sub.off('message', on); sub.unsubscribe(ch); };\r\n      },\r\n  };\r\n  useCluster(hub, adapter);",
									"lang": "javascript",
									"title": "Redis adapter (BYO ioredis)"
								}
							]
						}
					]
				}
			]
		},
		{
			"name": "WebRTC CLI",
			"description": "CLI subcommands for the `zh webrtc:*` namespace. Pure-function entry point `runWebRTCCommand(subcmd, flags, deps)` so the dispatch can be exercised in tests without spawning a child process or hitting the network.  All side effects (stdout / stderr / process.exitCode) are injected through `deps`, defaulting to the real globals when called from `lib/cli.js`.",
			"options": [
				{
					"option": "out",
					"type": "(line: string) => void",
					"default": "-",
					"notes": ""
				},
				{
					"option": "err",
					"type": "(line: string) => void",
					"default": "-",
					"notes": ""
				},
				{
					"option": "setExit",
					"type": "(code: number) => void",
					"default": "-",
					"notes": ""
				},
				{
					"option": "stunBinding",
					"type": "typeof defaultStun",
					"default": "-",
					"notes": ""
				}
			],
			"params": [
				{
					"param": "subcmd",
					"type": "string",
					"required": "Yes",
					"notes": "\nOne of `stun`, `turn-creds`, `join-token`, `verify-token`, `help`."
				},
				{
					"param": "flags",
					"type": "Map<string,string>",
					"required": "Yes",
					"notes": ""
				},
				{
					"param": "deps",
					"type": "object",
					"required": "No",
					"notes": "\nInjection seam for tests."
				}
			],
			"returns": {
				"type": "Promise<number>",
				"description": "The exit code that would have been set."
			},
			"example": "  // From the shell, via the top-level CLI:\r\n  //   npx zh webrtc:stun --host stun.l.google.com --port 19302\r\n  //   npx zh webrtc:turn-creds --secret $SECRET --user alice \\\r\n  //                           --servers turn:turn.example.com:3478\r\n  //   npx zh webrtc:join-token --secret $JT_SECRET --room lobby --sub u1\r\n  //   npx zh webrtc:verify-token --secret $JT_SECRET --token $TOKEN\r\n\n  // Programmatically:\r\n  const { runWebRTCCommand } = require('@zero-server/webrtc/cli');\r\n  await runWebRTCCommand('join-token', new Map([\r\n      ['secret', 's'], ['room', 'lobby'], ['sub', 'u1'],\r\n  ]));",
			"examples": [
				{
					"code": "  // From the shell, via the top-level CLI:\r\n  //   npx zh webrtc:stun --host stun.l.google.com --port 19302\r\n  //   npx zh webrtc:turn-creds --secret $SECRET --user alice \\\r\n  //                           --servers turn:turn.example.com:3478\r\n  //   npx zh webrtc:join-token --secret $JT_SECRET --room lobby --sub u1\r\n  //   npx zh webrtc:verify-token --secret $JT_SECRET --token $TOKEN\r\n\n  // Programmatically:\r\n  const { runWebRTCCommand } = require('@zero-server/webrtc/cli');\r\n  await runWebRTCCommand('join-token', new Map([\r\n      ['secret', 's'], ['room', 'lobby'], ['sub', 'u1'],\r\n  ]));",
					"lang": "javascript"
				}
			]
		}
	]
}
